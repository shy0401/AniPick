# Grading Risk Notes

## Node version

Use Node 22 LTS for frontend build verification on Windows.

## Generated files

`submission_outputs/` and the generated zip are submission artifacts but are intentionally ignored by Git.

## API keys

The project is designed to run with fallback data if API keys are absent.
