# Secret-Free Repository Check

The repository should not commit:

- `.env`
- real API keys
- generated zip files
- `node_modules`
- virtual environments
- build outputs

The package script also scans files before creating the submission zip.
