# Area Matching Policy

Area matching rewards restaurants whose `area` or `district` matches the parsed area.

For the assignment scenario, `??` and nearby `????` candidates are represented in the fallback dataset. The scoring function rewards exact area or district matches.
