# Test Coverage Summary

The test suite covers parser behavior, location resolution, scoring, tool observations, ReAct trace formatting, API compatibility, reflection checks, packaging filters, and dataset integrity.

Run:

```bash
python -m pytest -q
```
