# Memory Policy

The memory tool demonstrates the Memory Pattern with in-process storage.

It records the user query and parsed conditions for the current Python process. The storage is intentionally lightweight and is not persisted to disk.
